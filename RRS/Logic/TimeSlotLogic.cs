public static class TimeSlotLogic {
    public static void PrintTimeSlots(string date) {
        // foreach (ReservationTimeSlots reservationTimeSlots in Database.SelectReservationTimeSlots(date)) {
        //     Console.WriteLine($"{reservationTimeSlots.ID} - Start time:{reservationTimeSlots.StartDateTime}, End time:{reservationTimeSlots.EndDateTime}");
        // }
    }
}